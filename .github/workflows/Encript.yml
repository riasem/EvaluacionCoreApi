name: EncriptConfig
on:
  push:
    branches: ["integracion"]
jobs:
  encrip-config:
    runs-on: ubuntu-latest
    steps:
      - name: Configuraciones
        run: |
          echo ls
          echo gpg --version
          
          
      
      
      
